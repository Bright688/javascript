//create an array named colors that include list of colors
let colors=['red', 'green', 'blue'];
// add color to the end of the array
colors.push('black');
console.log(colors);
//remove the value red and swap the position of the value green and blue.
colors.shift();
colors.unshift('blue');
colors.unshift('yellow')
console.log(colors);